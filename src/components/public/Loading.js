const Loading = () => {
  return (
    <div
      style={{
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      l o a d i n g . . .
    </div>
  );
};

export default Loading;
