import PasswordChangeForm from '../components/Auth/password/PasswordChangeForm';
import PageTemplate from '../components/public/PageTemplate';

const PasswordChangePage = () => {
  return (
    <PageTemplate>
      <PasswordChangeForm />
    </PageTemplate>
  );
};

export default PasswordChangePage;
